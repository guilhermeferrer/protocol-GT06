const path = require('path');

module.exports = {
    config: path.resolve(__dirname, 'src', 'configs', 'postgres.js'),
    'migrations-path': path.resolve(__dirname, 'src', 'database', 'migrations'),
}